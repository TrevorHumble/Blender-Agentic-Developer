# Build log

Append-only record of the autonomous MVP build. Newest at the bottom.

- 2026-06-15 — Issue #0001 (system design documentation): 5-round adversarial issue review → PASS;
  Sonnet implementation → adversarial PR review 15/15 PASS → committed `e2fc83a`.
- 2026-06-15 — PLAN.md v1 → adversarial review FAIL (9 defects: author-blesses-own-work loophole,
  bootstrap ordering, circular reviewers, model contradiction, unreviewed reused skills, etc.).
- 2026-06-15 — PLAN.md v2 fixes all 9 (bootstrap section, no author override, dependency-layered
  order, stop-condition, model-deviation note). Build started.
- Process economy (orchestrator judgment, per Trevor "don't waste tokens"): per layer = one issue
  (orchestrator-written) → one Sonnet implementation reading prior art → one Sonnet adversarial
  artifact review → fix blocking defects → commit. No author override of any FAIL.
- 2026-06-15 — Layer A (#0002): cross-cutting standards. Review 15/15 PASS + 8 quality fixes. Committed `d9e916f`.
- 2026-06-15 — Layer B (#0003): authoring standards. Review FAIL (interpretive checklist items) → fixed → PASS. Committed `3485022`.
- 2026-06-15 — Layer C (#0004): 5 reviewer agents. Review FAIL (protocol off read-set; bias check not operationalized) → fixed → PASS. Committed `9176353`.
- 2026-06-15 — Layer D (#0005): 6 writer skills + researcher. Review FAIL (writer skills restated standards = bloat) → fixed → PASS. Committed `4f5bd01`.
- 2026-06-15 — Layer E (#0006): orchestrator + implementation-agent. Review FAIL (adjudicator reclassification loophole) → fixed → PASS. Committed `1a0a894`.
- 2026-06-15 — Layer F (#0007): Blender + ops skills. Review FAIL (wrapper drift; bash here-docs in PowerShell file) → fixed → PASS. Committed `8baabf9`.
- 2026-06-15 — Finalize (#0008): repo CLAUDE.md + README. Review FAIL (banned-word list restated) → fixed. MVP foundation complete.
- 2026-06-15 — [AUDIT] Doc-state audit: DESIGN.md had drifted (written pre-build). 8 drifts fixed → verified PASS. Committed `e5f8130`.
- 2026-06-15 — Future work (#0009 backlog + #0010 ready-issue): authored, run through reviewer-issue. FAIL twice (human-approval graduation, producer-POV stories, untestable outcomes, interpretive criteria) → fixed → round-3 PASS. Backlog/ready tier distinction established and honored.
- 2026-06-15 — `/build` slash command (#0011): issue → reviewer-issue FAIL (consumer POV, unfalsifiable AC, missing `---` fence check) → fixed → PASS → implemented `.claude/commands/build.md` → artifact review caught research-ordering + model-enforcement bugs → fixed. One-command orchestrator trigger.
- 2026-06-15 — B8 graduated: Trevor provided repo URL → `config/github.txt` written. Remote pushed to github.com/TrevorHumble/Blender-Agentic-Developer; actionable backlog filed as GitHub issues.
- 2026-06-16 — Efficiency research → adversarial assessment of the token-saving options (added implementation/maintenance-cost columns). De-biased assessors demoted E3/E4/E7 (cut content, not duplication) and found headroom worse than first assessed (telemetry to third-party Supabase, open issue #1006 silent data loss, Banner-ID compression). Caveman speak and headroom rejected for the critical path.
- 2026-06-16 — Finished the never-run DESIGN.md adversarial review (3 lenses): all FAIL. Real findings — same-tier author/reviewer (correlated blind spots), PR-review/minimum-context contradiction, adjudicator-downgrade wording stale vs orchestrator.md, plus doc drift. Acted: DESIGN.md currency fix (8 corrections, documentation gate PASS); filed #0012 (native caching), #0013 (reviewer independence), #0014 (efficiency adopt-with-conditions, low priority) through reviewer-issue.
- 2026-06-16 — #0010 (issue tiers: ready vs backlog) implemented. reviewer-pr round 1 FAIL (literal `implementation plan` case; dangling backlog-checklist reference) → fixed → round 2 PASS (one nit logged). Committed `df0b6a9`. Tier bars + graduation rule now in issue-standards; reviewer-issue applies the declared tier.
- 2026-06-16 — #0015 PREPARED (not implemented, per Trevor "prepare them"): self-improvement governance — folds the capture protocol (orchestrator captures system defects found mid-dev, fix-now vs backlog, never silently works around) with the self-modification bar (system-level changes need two independent reviewers). reviewer-issue (Opus) 3 rounds FAIL → 3-round cap → adjudicator READY. Key fix: removed a self-exemption bootstrap (governance-by-fiat) — the issue now binds itself to the two-reviewer bar it defines. Filed as GitHub issue #14, label `ready`. Awaiting Trevor's trigger to implement.
- 2026-06-16 — #0013 (reviewer model independence) implemented: reviewers + adjudicator now run on Opus (different model from the Sonnet implementer) to break correlated blind spots; independence rule in agent-standards, identical policy sentence in DESIGN.md + CLAUDE.md. reviewer-pr (run on Opus, dogfooding) round 1 FAIL (stale "(Sonnet)" at the execution point: orchestrator.md/build.md/PLAN.md — dependency map was incomplete; reconciled, Touches extended) → round 2 FAIL = reviewer correctly tripped its OWN bias gate (my briefing leaked the expected outcome; re-spawned de-biased) → clean round caught build.md:7 lead-paragraph contradiction (fixed) → next round escalated to stale model-policy prose in CLOSED issues #0006/#0008/#0011. Hit 3-round cap → independent adjudicator (Opus): #0013's 3 ACs PASS throughout; the closed-issue findings are retracted over-flags (cross-issue-consistency rule exists in no standard; closed issues are append-only historical contracts like this log). Ruling: MERGE-ELIGIBLE. Applied the adjudicator's non-blocking hygiene step: appended dated "Superseded by #0013" notes to #0006/#0008/#0011 (append, not rewrite). Committed.
- 2026-06-16 — #0025 (SECOND ADD-ON: phyllotaxis generator) IMPLEMENTED + VERIFIED LIVE. `addons/phyllotaxis.py` places N points in the golden-angle sunflower pattern (point i at angle `i × 137.50776°`, radius `scale × sqrt(i)`), in Add > Mesh > Phyllotaxis; optional dome falloff. bpy-free `phyllotaxis_points` (golden angle from `math.sqrt(5)`, count≤1 / count==0 guards). Issue review PASS (geometry verified). Live verify (AC6): count 300 / scale 0.1 → 300-vert "Phyllotaxis" mesh, every consecutive angle 137.508° — the iconic interleaved spiral (screenshot). Artifact review ran BOTH gates: reviewer-pr 6/6 ACs PASS; reviewer-design-philosophy PASS (zero red flags — deep `phyllotaxis_points`, errors defined out for the degenerate counts). The upgraded pipeline produced a second clean, beautiful, verified add-on.
- 2026-06-16 — #0015 (self-improvement governance) IMPLEMENTED — closes the self-improvement loop. (a) Capture protocol: `agents/orchestrator.md` now has a "Capturing a system defect mid-run" section — when a system defect surfaces during a run the orchestrator must `do not silently work around` it, `capture it as an issue` routed `issue → review`, deciding `fix-now` (blocks the current task → ready issue, pause/fix/resume) vs `backlog` (the run `does not derail`); new `skills/capture-system-defect.md`. (b) Self-modification bar: `standards/adversarial-review-protocol.md` now requires `two independent reviewers` for a `system-level change` (`independent of each other and of the implementer`; `disagreement is treated as FAIL`), with `DESIGN.md` the canonical `system-level change` definition. Reviewed UNDER ITS OWN BAR (two independent Opus reviewers): round 1 — A PASS, B(coherence) FAIL blocker (the new 2-reviewer rule contradicted the existing ≥3-adversary high-stakes rule) + major (two divergent system-level definitions) → reconciled (explicit exception; one canonical definition) → round 2 — A(coherence) PASS, B FAIL major (unannounced Touches edit to spawn-adversarial-review) → amended → final: BOTH PASS. The second reviewer caught what the first missed every round — the bar proving its own worth on the issue that creates it.
- 2026-06-16 — #0024 (CI on GitHub Actions) IMPLEMENTED — graduates B2. `.github/workflows/ci.yml` runs on every push/PR (ubuntu-latest, public repo, unlimited minutes): bpy-free pure test → download free Blender 5.1 (verified 200) → headless operator test → eval suite, each `--python-exit-code 1` exit-gated. In-license: no Codecov/Coveralls/hosted SaaS, free Blender, no non-Anthropic model. reviewer-pr PASS (verified exit-code propagation, live download URL, YAML 7 steps, forbidden literals absent). "Trevor can't audit every change" is now enforced by the repo. (Orchestrator verification: first run on push SUCCEEDED — green in 34s; downloaded Blender 5.1, ran pure + headless + eval suite, all passed in a clean Actions environment. The whole automated pipeline works end-to-end.)
- 2026-06-16 — #0023 (eval harness) IMPLEMENTED — the eval layer Trevor asked for, in-license (Claude-as-judge + headless geometry + no external eval SaaS). `evals/cases.py` (5 graded geometry cases: square, triangle, open polyline, colinear-skipped, large-radius-clamped), `evals/run_evals.py` (runs the suite headless, prints `EVAL SCORE: P/T`, exits 1 on any failure), `evals/judge.md` (the Claude-as-judge / MCP-side eval: viewport screenshot → Opus judge against a rubric), `evals/README.md`. Orchestrator verification: live headless run = `EVAL SCORE: 5/5`, exit 0 — the bevel add-on passes all five cases. reviewer-pr PASS, and CRITICALLY proved the checks are NON-VACUOUS by tamper-testing (broke the add-on 3 ways — no-split → 0/5, unclamped overlap → 4/5 via the bounds check, injected NaN → 4/5 via the finite check). Added `.gitignore` (__pycache__, .review_state).
- 2026-06-16 — #0022 (headless Blender test harness) IMPLEMENTED — graduates #0017, and the first add-on (#0018) being committed met #0017's graduation trigger. `tests/run_pure.py` (bpy-free geometry via a stubbed bpy, plain Python), `tests/run_headless.py` (bpy operator under `blender --background --python`), `tests/run_tests.ps1` (runs both, exit 1 if either fails). Dependency-free (no pytest/pip → in-license). Orchestrator verification (recorded): the harness runs green end-to-end (PURE_PASS + HEADLESS_PASS + ALL_TESTS_PASS, exit 0); a bogus Blender path exits 1; a raising headless script exits 1. reviewer-pr found and the loop fixed two consequential false-pass blockers: (1) a missing Blender/Python path silently inherited a stale exit 0 → added Test-Path/Get-Command guards; (2) Blender background mode exits 0 even on an unhandled Python exception by default → added `--python-exit-code 1` (verified: raising script now exits 1, was 0). Round 3 PASS. The Blender domain now has a real test gate; unblocks CI (B2).
- 2026-06-16 — #0021 (Ralph-loop enforcement via a Claude Code Stop hook) IMPLEMENTED — graduates B3. New `.claude/hooks/review-gate.ps1` + `.claude/settings.json`: the Stop hook emits `{"decision":"block","reason":...}` to re-inject the task unless a verdict artifact shows a legitimate exit (`PASS` or #0020's `EXIT_AUTHORIZED`), guarded by `stop_hook_active` and a `MAX_ITERS` counter backstop. Researched via claude-code-guide (authoritative hooks contract); fully in-license (local script, no API). Orchestrator runtime verification (recorded): (a) no verdict → block JSON; (b) EXIT_AUTHORIZED → allow + verdict cleared; (c) stop_hook_active → allow. THIS RUN DOGFOODED #0020 END-TO-END: reviewer-pr found a cascade of consequential fail-open safety defects (stale-verdict not cleared, missing-cwd, corrupt counter, empty-stdin→null, unwritable counter) — each round the loop CONTINUED and the implementer hardened the hook fail-safe (verified live each time); reviewer-design-philosophy PASS after the stale-verdict fix. After convergence, the new `severity-adjudicator` was invoked and ruled the two remaining defects (whole-string vs contains match; case-insensitivity) inconsequential — one fails safe, one reachable only via out-of-scope wiring → **EXIT_AUTHORIZED**. Committed. (Follow-up when the orchestrator-writes-verdict wiring lands: use a first-token/contains match for real multi-line verdicts.)
- 2026-06-16 — #0020 (soft cap + external severity gate) IMPLEMENTED — replaces the hard 3-round cap per Trevor. The 3-round mark is now a TRIGGER, not a hard cap: an independent `severity adjudicator` (new agent, Opus, context-free per round) classifies every remaining defect `consequential`/`inconsequential` and may authorize exit ONLY when all are inconsequential; the loop never accepts work while a consequential defect remains; author/implementer/orchestrator never classify severity. Termination guaranteed without bad self-exit: a consequential defect surviving the gate + 3 further rounds = impasse → halt and surface to the operator (a halt is not an acceptance; the orchestrator owns the post-gate counter). Touched protocol + DESIGN + CLAUDE + orchestrator + new severity-adjudicator.md; reconciled the executable build.md and PLAN.md (dependency map had missed them). reviewer-issue 2 rounds → PASS. Artifact review DOGFOODED the two-reviewer bar: the 2nd reviewer caught a BLOCKER the 1st missed — build.md (the live /build command) still ran the old hard-cap mechanism — plus an AC9 case mismatch → fixed → round 2 PASS (0 defects). Committed.
- 2026-06-16 — #0019 (in-license constraint) IMPLEMENTED. Audit confirmed the repo is 100% within GitHub Pro + Anthropic Max with no outside APIs. Encoded the constraint as DESIGN.md governance (Anthropic-only models; local-only Blender — prohibited asset generators named; CI on GitHub Actions/public repo; Claude-as-judge + pytest + headless eval stack), a CLAUDE.md operating rule, and an `out of license` FAIL check in issue-standards so reviewer-issue enforces it going forward. reviewer-issue 2 rounds → PASS; reviewer-pr PASS. Committed.
- 2026-06-16 — #0018 (FIRST REAL ADD-ON: bevel/round bezier corners) IMPLEMENTED end-to-end and VERIFIED LIVE in Blender. `addons/bevel_bezier_corners.py` rounds a curve's sharp corners into true circular-arc bezier fillets (deep `rounded_corner` geometry: setback `d=r/tan(θ/2)`, overlap clamp, `(4.0/3.0)·tan(α/4)·r_eff` arc handles, colinear guard). Issue review (Opus) 3 rounds → PASS. Implementation: Sonnet wrote it + unit-tested the bpy-free geometry; orchestrator verified AC7 live — cyclic POLY square → cyclic BEZIER, 8 points at (±0.6,±1)/(±1,±0.6), arcs confirmed by screenshot; open-curve endpoint path also verified (3-pt → 4 pts). Artifact review ran BOTH gates: `reviewer-pr` PASS (7/7 ACs); the new `reviewer-design-philosophy` gate FAILed on real code smells (duplicated vector helpers, None-sentinel two-pass leak, dict/namedtuple inconsistency) → 2 refactors (single helper family, namedtuple records, extracted `_write_bezier_spline`, removed dead `arc_side`) → 3-round cap → adjudicator MERGE-ELIGIBLE (findings genuinely fixed, two-phase split is essential complexity not temporal decomposition). The design-philosophy gate proved its worth on first contact with real code. Committed. (This is #0017's graduation trigger — the headless-test-harness backlog is now graduation-eligible.)
- 2026-06-16 — #0016 (regular adversarial architectural + design-philosophy review) IMPLEMENTED. New: `standards/design-philosophy.md` (Ousterhout: deep modules, info hiding, pull complexity down, etc. + red flags), `agents/reviewer-architecture.md` (judges fit vs DESIGN.md, no duplication), `agents/reviewer-design-philosophy.md` (judges artifacts vs the standard). Even/odd cadence rejected → trigger-based standing gates: architectural review at issue time for system-level/component-adding changes; design-philosophy review after reviewer-pr PASS for every (non-doc) implementation artifact; `[AUDIT]` full-system architectural audit every 5th counted BUILDLOG entry. Supersedes #0009 B1. Issue review: 3 rounds → PASS. Artifact review DOGFOODED #0015's two-reviewer bar (two independent Opus reviewers in parallel): one PASS, one FAIL with 3 real coherence majors the AC-only reviewer missed (standard-vs-reviewer severity contradiction; recursive/ambiguous audit counter; undocumented doubling of per-PR review cost) → fixed → round 2 FAIL (audit-counter self-contradiction reintroduced; add/adds drift) → fixed → round 3 PASS. Committed.
- 2026-06-16 — #0012 (native prompt caching, E2) implemented. reviewer-pr 3 rounds all FAIL (R1: unsourced platform-internal claims + missing token-minimum; R2: step-1 finding unrecorded + no citations; R3: cited URL alleged malformed) — converging, findings narrowed each round. Hit 3-round cap → independent adjudicator: verified the URL actually resolves (`docs.anthropic.com` 301-redirects to the cited `platform.claude.com` page), ruled the R3 blocker a false positive, all 4 ACs met, MERGE-ELIGIBLE with cited basis. Committed. Stable-prefix caching discipline (ordering-only, content-hash invalidation, cache_read_input_tokens verification, token-minimum precondition) now in spawn-adversarial-review.
- 2026-06-16 — #0026 (documentation-currency reviewer, graduates B5) IMPLEMENTED — the agent that would have caught tonight's README/index drift automatically. New `agents/reviewer-doc-currency.md` (Opus, `tools: [Read]`): judges a PR **diff** (not a single file) and FAILs when a currency-triggering change (new/moved top-level dir, new agent/skill/standard, renamed interface/path) lands without its matching front-door doc updated in the same diff (`README.md` layout, `DESIGN.md` structure map, `CLAUDE.md` roster). Explicitly distinct from `reviewer-documentation` (which judges one doc and can't see cross-file staleness). System-level change → reviewed UNDER the two-independent-reviewer bar. Issue review (reviewer-issue) PASS (one minor folded in: dropped a loosening parenthetical so AC4 is fully literal). Artifact review round 1: A PASS, B FAIL major — DESIGN.md/CLAUDE.md prose said the gate "fires" in present tense, implying live wiring, but orchestrator wiring is explicitly deferred to a follow-up (the gate is inert). Exactly the doc-currency defect this agent exists to catch — caught by the disagreement=FAIL rule, on the agent that catches it. Fixed (both docs now mark it existing-but-not-yet-wired) + stale B5 header ("enforcement"→"currency"). Round 2: BOTH reviewers PASS (nits only). Committed.
- 2026-06-16 — [AUDIT] Adversarial roadmap/backlog audit (Trevor: "any glaring issues because they aren't completed"). Found the roadmap was NOT honestly representing its own state: (1) #0021's Stop hook ships enforcement but nothing writes the `verdict.txt` it gates on — orchestrator.md has zero `verdict` refs (fails open at MAX_ITERS, so wasteful not fatal); (2) four reviewers (`reviewer-skill/agent/documentation/doc-currency`) exist but orchestrator.md never spawns them — inert gates; (3) #0024 graduated B2 but skipped its ruff + ≥80%-coverage outcome; (4) phyllotaxis shipped with zero eval coverage; (5) the GitHub tracker was comprehensively stale. Verified the two blockers by grep. Captured as backlog B9–B13 (capture protocol, backlog tier — they don't block the audit task), de-staled PLAN.md. Did NOT auto-wire the orchestrator (consequential machinery → surfaced for Trevor). Committed `c04af07`.
- 2026-06-16 — #0028 (GitHub is the single source of truth) IMPLEMENTED — REVERSES #0027 per Trevor (he wants one place to see everything; GitHub is easier than reading local files). Fix removes the drift risk #0027 reacted to by ADDING the maintaining step #0027 lacked, not by abandoning the board: `DESIGN.md` "Source of truth" now declares GitHub canonical for task status with the board kept in sync by the pipeline (file = detail, issue = status); `skills/github-write.md` restores the `gh issue create`/close + sync discipline; `agents/orchestrator.md` + `.claude/commands/build.md` open a card on issue-create and close it on commit; new `agents/reviewer-tracker-sync.md` (Opus, read-only) FAILs any merge that leaves the board out of sync; repo + GLOBAL `~/.claude/CLAUDE.md` carry the rule (the latter as a cross-project Blender convention); #0027 marked Superseded, B12 reversed. Board rebuilt to match a verified reality: ran FOUR adversarial status auditors (every issue #0001–#0027 + backlog B1–B13) + THREE feature-reality verifiers (actually ran the add-on/eval math; traced whether each gate is truly wired vs an orphaned file — confirmed only the 3 known loose ends B9/B10/B11 are inert, no new surprises). Backfilled #0001–#0008 + filed every missing shipped issue as CLOSED; reopened B4/B6/B7, filed B9/B10/B11/B13 + #0014 OPEN; ECs CLOSED as NOT_PLANNED (consolidated into open #0014). Final board: 9 open (the real remaining work) / 28 closed. System-level change → two-independent-reviewer bar: round 1 BOTH FAIL (build.md missing the sync step that orchestrator.md had = AC5 blocker + loop-spec contradiction; ECs closed-as-COMPLETED when unfinished) → fixed (build.md synced; ECs re-closed NOT_PLANNED; #0009 backlog-container exempted in the sync gate) → round 2 BOTH PASS. The reversal is itself the first thing tracked under the new GitHub-canonical regime.
- 2026-06-16 — [REVIEW] Two-round major adversarial review (8 independent agents each, whole-repo, told to distrust the repo's own claims and research practices externally). Round 1 found the project over-built relative to its output (two ~250-line add-ons under thousands of lines of governance), with several gates inert (dead Stop-hook, fictional coverage gate, 4 unwired reviewers, count-only tests). Round 2 (re-judged against Trevor's real goal: a non-developer shipping maintainable/scalable software via AI, who needs gates to do code-checking he can't) challenged the team's own rebuttal conclusions and converged on: go PRODUCT-FIRST (the SVG editor proves which gates are real); context-isolation fights anchoring not same-model correlation; coverage-% is a vanity metric (use tamper/mutation tests); turn on the free GitHub security layer; consider an occasional human code audit for anything meant to scale. Reports: `reviews/2026-06-16-major-adversarial-review.md`, `reviews/2026-06-16-round2-challenge-review.md`.
- 2026-06-21 — [ENGINE] Baked the autonomous self-enforcing loop into `agents/orchestrator.md` (the thing that frees Trevor from enforcing). Two permanent rules: (1) **Self-review is automatic** — producing any artifact auto-fires its adversarial review (two reviewers fail-closed for system-level), "done" = "passed review", producer never reviews, no human prompt needed; (2) **Autonomous timed run** — a time-driven, not task-driven, loop: self-timing via an emitted per-increment ledger line, a next-action selector that can never return "stop" before the time budget, a Done-Early Cascade (holistic review → revisit blockers → deep research → raise the bar → weed issues) that refills its own queue, blockers re-verified not parked, deep web research promoted to a default in idle time, and a bounded emergency stop-list (irreversible/destructive, out-of-license, security, owner-only scope). `docs/AUTONOMOUS-RUN.md` reduced to working state pointing at the canonical spec. Built the right way — and dogfooded it: I auto-spawned the review without being asked; round 1 BOTH reviewers FAIL (impasse-halt was a second early-exit door; "never stop" had no emergency brake; the loop was prose not an emitted mechanism; the self-review line contradicted the orchestrator's own write-scope) → fixed every finding → round 2 BOTH PASS. The loop proving itself on the change that creates it.
- 2026-06-16 — [ACTION] Acted on the review's safe, high-value, non-governance items while Trevor was out. Enabled the free, in-license GitHub security layer (Secret Scanning + push protection, Dependabot alerts + security updates, CodeQL default setup; `.github/dependabot.yml`); protected `main` (no force-push/delete); clarified the in-license rule so "no SaaS" no longer excludes first-party GitHub security tools. Filed the round-2 path as GitHub issues GH#40–46 (product spike, output-correctness tests, tamper gate, handoff docs, honesty fixes, two Trevor-decision issues) and GH#47 (security, closed). No governance was removed and no architecture decision was made unilaterally — those await Trevor.
- 2026-06-16 — #0027 (declare BUILDLOG + issues/ canonical; stop GitHub issue mirroring) IMPLEMENTED — graduates B12; Trevor chose Path B via adversarial root-cause diagnosis. Root cause: the GitHub mirror was a one-time manual push (B8) that nothing maintained — no pipeline step writes/closes a GH issue. Fix removes the drift surface rather than adding a sync surface: `DESIGN.md` "Source of truth" section declares BUILDLOG + `issues/` canonical and the board archived read-only; `skills/github-write.md` loses its `gh issue create` path (the single invariant: no documented way to write GH issues survives); CLAUDE.md/PLAN.md updated. Closed all 16 GitHub issues (9 shipped → "shipped as #NNNN" pointer; 6 still-open-locally backlog → "archived, remains open in issues/0009"; #0018 was already closed) — `gh issue list --state open` now returns `[]`. System-level change, reviewed under the two-reviewer bar: issue review FAIL (BUILDLOG missing from Touches; AC6 lacked an absence check) → fixed → PASS. PR round 1: C PASS, D FAIL major — a hard line-wrap split the literal `not mirrored to\nGitHub` across two physical lines, so AC1's prescribed grep failed (C eyeballed it as present; D ran the grep — the bar catching what single review missed, again). Rewrote so one line carries all three literals → round 2 BOTH PASS. Committed.
- 2026-06-21 — [GATES] #41 geometric-correctness tests, both shipped add-ons. Autonomous timed run (start 18:15, 120m budget) opened with a self-fired architecture + quality review (self + two independent Opus adversaries, told to distrust the docs); both converged on the same punch-list (phyllotaxis had ZERO tests; bevel tests read tangent points but ignored handles, so a flat chamfer passed as a circular fillet; no mutation harness; coverage-80% fiction; CodeQL asserted in the log but absent from the repo). Fixes, each delegated to an implementation agent then independently Opus-reviewed PASS: (1) `tests/run_phyllotaxis_pure.py` — asserts the golden-angle invariant (independently re-derived; catches a 137.5°/radian mutant at ~135× tolerance), the scale·√i radius law, edge cases, and the dome paraboloid; wired into `run_tests.ps1` + CI. Committed `9c295b6`. (2) `tests/run_pure.py` extended — asserts handles, handle length/direction, and the decisive cubic-bezier-midpoint-on-arc property (rejects a chamfer by ~58×), plus an acute 60° case; tamper-verified (h=0 and 4/3→1.0 both caught). Closes #41.
- 2026-06-21 — [GATES] #42 mutation/tamper harness — the North Star "smoke detector" (the thing that lets a non-coder believe a green light). `tests/mutation_harness.py` deliberately breaks copies of both add-ons — 6 mutants: bevel chamfer (h=0), wrong-arc-constant, wrong-setback; phyllotaxis golden-angle-hardcode, radius-linear, dome-sign — runs the matching bpy-free pure test against each via a path-override env var, and asserts the test FAILS, emitting `guards caught N/N`. A mutant the suite still passes is reported SURVIVED (a blind gate) and the run exits non-zero. Hardened after review so a "catch" is credited ONLY when the test's own assertion fired (success sentinel absent AND a FAIL line present); a mutant that merely crashes for the wrong reason is reported ERRORED, not caught. Proven teeth in BOTH directions: a no-op probe is correctly SURVIVED and a load-crash probe correctly ERRORED (each → exit 1), so the harness cannot be vacuously green. Pure Python (no Blender); wired into CI and `run_tests.ps1` as a blocking gate; empty-mutant-list guard closes the 0/0 trivial-green path. Two independent Opus reviews PASS (structure, then the hardened classifier). Closes #42.
- 2026-06-21 — [HONESTY] #44 removed the coverage fiction. DESIGN.md claimed code "requires ≥ 80% test coverage" and listed `pytest`/`coverage.py` in the eval stack, but nothing measured coverage and the harness is dependency-free — false confidence for a code-blind owner. Replaced with the real bar: a "Test strength" section stating tests must assert actual output VALUES and the mutation/tamper harness must catch deliberately-broken code (no line-coverage threshold is measured or required); fixed the PR-lifecycle reviewer-checks line; corrected the eval-stack sentence and the CI gate list to the five real gates; relabeled "CI/coverage enforcement" → "CI enforcement". `addons/README.md` now points each add-on at its real test file (the phyllotaxis "unit-tested outside Blender" line — false when #41 was filed — is now true, since `run_phyllotaxis_pure.py` exists). System-level change → two independent Opus reviewers; both caught the SAME residual: `agents/implementation-agent.md` still told the implementer to run "coverage checks as required by the PR lifecycle's coverage requirement" → fixed to name the actual test gates. Residual noted, NOT chased (it's a closed historical issue file): `issues/0001` still lists "80% coverage" as an acceptance criterion. BUILDLOG scanned clean of marketing slop. Closes #44.
- 2026-06-21 — [ENGINE] Hardened the autonomous-run timing in `orchestrator.md` after living the failure mid-run. The never-stop loop's WRAP decision depends on elapsed minutes, but the spec only said "emit elapsed=Xm" — so the operator estimated by feel and drifted ~11m fast (logged 25m when the true clock-measured elapsed was 13.7m), which would trip an early WRAP and stop the run before its budget. Fix-for-good: the start timestamp and every ledger line's `elapsed` must now be READ from a real system clock (`Get-Date -UFormat %s` / `date +%s`), computed `(now−start)/60`, never estimated/inferred/carried-forward; any non-clock-derived ledger line is invalid; and the `elapsed` driving EVERY selector/WRAP decision must come from a fresh read, not the last ledger number. System-level change → two independent Opus reviewers, both PASS; both flagged the same non-blocking residual (the cosmetic `[HH:MM]` field is still hand-typable — my own log shows `[18:26]→[18:24]` going backwards — but it cannot cause an early WRAP because the selector keys off the now-clock-bound `elapsed`) → filed as backlog. Verified the security state read-only while here: Secret Scanning + push protection + Dependabot security updates all ENABLED; CodeQL returns zero analyses (a claim-vs-reality gap vs the 2026-06-16 [ACTION] entry) → filed for Trevor (enabling/changing it is a security setting, his call); no pip dependency manifest exists, so a pip Dependabot ecosystem is N/A.
- 2026-06-21 — [GATES] Raised the mutation harness's rigor (Done-Early Cascade output). A deep-research agent surveyed Python mutation-testing practice and recommended KEEPING the hand-rolled curated harness over mutmut/cosmic-ray — Windows/`fork` limits (the owner couldn't run them locally), auto-generated-mutant triage burden, and the loss of the auditability a non-coder needs — and banking two dependency-free upgrades instead. (4a) Per-mutant catch attribution: each mutant declares the SPECIFIC FAIL line it must trip (`expect_fail`); a failure from a DIFFERENT assertion is MIS-CAUGHT and does not count, so "caught" now means the RIGHT guard fired, not merely that something failed. (4b) An explicit `MUTATION_SCORE: caught/survived/errored/mis-caught/broken score=N/N` line with split counters (the verbatim `guards caught N/N` phrase preserved for the docs). Proven: MIS-CAUGHT teeth shown (a wrong expected-assertion → 5/6, exit 1); a candidate off-by-one mutant was correctly REJECTED — it crashes with IndexError before the count check, so it's `errored`, not a clean catch. Independent Opus review PASS; applied its future-proofing note (tightened the setback mutant's substring to `: t1 expected` so a reordered test can't mislabel a handle failure as a setback catch).
- 2026-06-21 — [GATES] Upgraded the headless Blender test (`tests/run_headless.py`) from count-only to position-asserting — closes a finding from BOTH the architecture and quality reviews (the old test checked "8 bezier points exist," so an operator that placed all 8 at the origin or used the wrong radius would pass). It now asserts each of the 8 bezier knot `.co` against ground truth computed from the SAME pure `rounded_corner` the operator calls internally — so the expected values were verified locally WITHOUT Blender (the pure function is already tested by run_pure.py), with an order-independent fallback so a mere point-ordering difference can't red-fail CI while wrong geometry still fails. Independent Opus review PASS: recomputed the 8 positions (match to ~1e-16) and confirmed the fallback rejects all-at-origin / duplicate / wrong-radius outputs. Blender-side verification is by CI (confirmed green).
- 2026-06-21 — [GATES] Upgraded the eval suite (`evals/cases.py`) from count-only to position-asserting on its two count-only cases. Added `_check_square_positions` (8 knots) and `_check_triangle_positions` (6 knots — the only end-to-end positional assertion at ACUTE, non-90° corners), each comparing the operator's Blender output against ground truth computed OFFLINE from the verified pure `rounded_corner` (hardcoded, independent of the add-on under test, since cases.py runs under Blender), using the same order-independent matcher as `run_headless.py` (length-first, each produced point consumed once — proven to reject all-at-origin / duplicate / wrong-radius). Independent Opus review PASS (recomputed all 14 points to 0.0 diff; 7 adversarial false-pass cases all rejected); the orchestrator independently recomputed the triangle too. Milestone: EVERY gate — pure, mutation, headless, and eval — now asserts real geometry, not just point counts. Blender-side verified by CI.
- 2026-06-21 — [GATES] Added the phyllotaxis OPERATOR test (`tests/run_phyllotaxis_headless.py`), closing the symmetric gap a completeness audit (and #35) flagged: the phyllotaxis pure function was tested, but the operator that actually builds the mesh in Blender was not — bevel's operator had headless + eval coverage; phyllotaxis had neither. It runs `mesh.add_phyllotaxis` under headless Blender and asserts the created mesh's 50 vertices match `phyllotaxis_points(50,0.1,2.0)`, PLUS independent geometric anchors that hold even if the pure function were swapped: vertex 0 on the Z axis (x=y=0 — correctly NOT (0,0,0), since dome=2.0 lifts it to z=2.0, a subtlety the implementer caught) and the scale·√i radius law at i=10,49. Wired into CI and run_tests.ps1. Independent Opus review PASS. Blender-side verified by CI. Substantively addresses #35 (operator half).
- 2026-06-21 — [HONESTY/DOCS] Completeness-critic follow-ups. (1) Corrected DESIGN.md, which still described the Stop-hook (`review-gate.ps1`) as LIVE enforcement — the same class of defect #44 fixed — when this run's own #46 evidence proved it inert (nothing writes the `verdict.txt` it gates on; every Stop falls through to the `MAX_ITERS=25` backstop, which fails open and would also terminate a long autonomous run early). Annotated three spots to mark it inert pending the #46 governance decision, keeping the intended-design description but stating the live status plainly. System-level → two independent Opus reviewers, both PASS (each independently confirmed nothing writes the verdict file and the hook is registered-but-inert). (2) Refreshed `tests/README.md` (stale: it listed 2 of the now-6 test entry points) to describe every gate and which need Blender, and added the eval suite to `tests/run_tests.ps1` so the local runner matches CI (it was CI-only). NOT touched: the hook itself (governance → #46) and ci.yml.
- 2026-06-21 — [GATES] Completed positional coverage of the eval suite — added `_check_large_positions` to the `large_radius_clamped` case (the last count/bounds-only check). It verifies the CLAMP path produces exactly-correct geometry: at radius 5.0 clamped to half-edge 1.0, all 8 knots land on edge midpoints (4 distinct locations, each shared by adjacent corners), computed offline from the verified pure `rounded_corner`. The existing order-independent matcher handles the legitimately-duplicate expected values via consume-once. A clamp bug producing within-bounds-but-WRONG positions (which the prior bounds check accepted) now fails. Independent Opus review PASS (recomputed exact; duplicate-safety proven against four adversarial sets — correct→PASS, quadrupled→FAIL, all-origin→FAIL, 7+1→FAIL); the orchestrator independently recomputed too. Blender-side verified by CI. Every eval case now asserts exact positions.
- 2026-06-21 — [GATES] Added a deterministic lint + security CI gate (#32 remaining scope), strengthening the thin non-LLM quality layer the architecture review flagged. A new parallel `lint` CI job (plus a guarded local-runner block) runs **ruff** (`--select F,B` — Pyflakes + bugbear: unused imports/vars, undefined names, mutable-default args) and **bandit** (`-r addons/` — security anti-patterns: eval/exec/subprocess/pickle/secrets), both scoped to the shipped `addons/` only (not the harness, whose asserts would trip bandit). This converts two judgments that were 100% LLM-opinion — code cleanliness and security patterns — into checks that run identically every build; bandit also complements the pending CodeQL (#48) as the fast pattern-pass layer. Verified clean BEFORE wiring (and again by the reviewer, who ran the tools): ruff "All checks passed!", bandit "No issues identified" on 331 LOC / 0 issues — so it ships green, no add-on code changed and no rule suppressed. Chosen from a researched menu (ruff + bandit ADOPT-NOW; mypy/vulture ADOPT-LATER pending type annotations / a bpy whitelist; radon SKIP — full menu on #32). Independent Opus review PASS (YAML valid, scoped, `test` job untouched, local runner skips gracefully when the dev tools are absent).
- 2026-06-21 — [BUGFIX] Real defects from a 12-expert holistic review (Trevor: keep going, fix what's goal-aligned). (1) The bevel operator could corrupt curve data in Edit Mode — `poll()` (operator + panel) now requires OBJECT mode, since `execute()` rebuilds splines via the data API which is unsafe in Edit Mode (confirmed against the Blender 5.x RAG). (2) `rounded_corner` silently emitted a bogus fillet on a zero-length edge (duplicate adjacent vertex): `_normalize` returned the zero vector, fooling the colinear guard into theta=π/2 — now returns None (degenerate) before the dot/acos; added a run_pure.py test. (3) The phyllotaxis HEADLESS gate tested the add-on AGAINST ITSELF — its `expected` came from the same `phyllotaxis_points` under test, so a wrong golden angle or dome would pass index-by-index; added INDEPENDENT in-file anchors (golden-angle step + dome paraboloid, re-derived not imported) that reject a 137.5° constant and a flipped dome. Two independent Opus reviews PASS each; valid geometry unchanged, mutation harness still 6/6. CI-verified.
- 2026-06-21 — [BUGFIX] Bevel large-radius clamp no longer emits degenerate coincident knots (holistic-review finding; the eval had been CERTIFYING the degenerate output as correct). When the radius exceeds half an edge, adjacent fillets meet at the edge midpoint — the old code emitted that as two coincident bezier knots (a zero-length segment + a collapsed handle). Added `_merge_coincident_knots` (per-spline, cyclic-aware, eps 1e-7): merges adjacent coincident knots into one carrying the incoming handle from one side and the outgoing from the other, so the curve stays smooth with no zero-length segments. For the radius-5.0 square the 8 knots collapse to the 4 edge midpoints — a clean inscribed shape with proper 4/3·tan(π/8) arc handles. Non-clamped geometry is byte-identical (the merge is a no-op when nothing coincides: radius-0.4 square/triangle/open/colinear unchanged, run_pure PURE_PASS, mutation 6/6). Eval `_LARGE_EXPECTED_CO` recomputed to the 4 clean points; `_check_large_8pts`→`_check_large_4pts`. Independent Opus review PASS (cyclic wrap, open endpoints, tiny-square no-over-merge, 2-knot collapse all handled). Blender eval verified by CI.
- 2026-06-21 — [GOVERNANCE] Two system-level resolutions from the holistic review, decided against Trevor's goals (he said: answer your own questions). (1) DELETED the dead Stop-hook — `.claude/hooks/review-gate.ps1` + its settings.json registration removed; DESIGN.md updated from "inert pending #46" to DELETED. It gated on a verdict file nothing ever wrote, so it enforced nothing and only ever hit its `MAX_ITERS=25` fail-open backstop — which also CONTRADICTED the never-stop loop (a hard cap would kill a long autonomous run); the loop has zero dependency on it. An orchestration-systems consultant ruled DELETE. (2) RECONCILED the "out of the operational loop" vs "final eye" contradiction across CLAUDE.md, DESIGN.md, orchestrator.md, and PLAN.md: no human reads CODE in the critical path (the adversarial reviewers are the code gate), but Trevor IS the final eye on the visual/aesthetic result and the director — and orchestrator.md now PERMITS a future "Trevor confirms the visual result" checkpoint (anticipated, not yet built) instead of forbidding any human step. Routed through an implementation agent, not the orchestrator's own pen — the producer-never-reviews fix the LLM-orchestration expert recommended for governing docs. System-level → two independent Opus reviewers: round 1 BOTH FAIL on the same residual line (DESIGN.md's unscoped "Human-out-of-loop" Core Principle still contradicted the reconciliation) → rescoped to "Human-out-of-the-code-loop" → round 2 BOTH PASS. Also untracked a stray committed `.pyc`. Closes #46.
- 2026-06-21 — [PACKAGING/CI] Two additive goal-aligned items from the holistic review. (1) Added Blender Extensions manifests (`addons/<name>.blender_manifest.toml`, kept as siblings of the .py so the test loaders' exact-path references stay valid) — Goal 2 "ready to share": the add-ons were legacy `bl_info`-only scripts, not installable on the Blender 5.x Extensions platform; the manifests carry the required schema (id/version/name from bl_info, `type="add-on"`, `blender_version_min="4.2.0"`, SPDX GPL-3.0-or-later, valid tags), confirmed against the Blender RAG. `bl_info` kept as the legacy fallback. (2) Cached the Blender download in CI (`actions/cache@v4` keyed on the version; the download step guarded to run only on a cache miss; an always-run "Verify Blender" step checks the binary on BOTH hit and miss paths) — removes the false-red risk of re-fetching ~300MB from download.blender.org every run and speeds the pipeline. Independent Opus review PASS (manifests valid, cache logic correct, no regression, no red-CI risk). CI-verified.
- 2026-06-21 — [PROCESS] Baked a goal-first decision rule into the global `~/.claude/CLAUDE.md` and `agents/orchestrator.md` after Trevor flagged that I'd surfaced four technical questions all answerable from the goals — and one literally asked permission to continue work he'd already authorized. The rule: before surfacing ANY decision, derive it from the goals/constraints; if the goals/CLAUDE.md/an instruction settle it or it's a technical tradeoff, decide and act; only ask for irreversible owner-exclusive actions the goals don't decide; never ask permission for authorized work; a consultant resolves a call, it doesn't hand it back. Root cause: conflating "the goals decide it" with "the owner decides it," and reaching for "ask" as a safety blanket under uncertainty/fatigue. The four parked questions resolve from the goals: enable CodeQL (Goal 2 deterministic security + CLAUDE.md requires it), build the visual-confirmation gate (Goal 1 final eye), make the green light actually gate merges (Goal 1 "believe the green light"), keep going (already authorized).
- 2026-06-21 — [SECURITY] Enabled CodeQL via a committed advanced-setup workflow (`.github/workflows/codeql.yml`) — the deterministic static-security scanner CLAUDE.md declared REQUIRED and an old log wrongly claimed was on (zero analyses existed; #48). Python, `security-extended` suite (covers `py/xxe`/`py/xml-bomb` for the future SVG editor's untrusted-XML surface, #40), least-privilege permissions, on push/PR/weekly. Closes the claim-vs-reality gap and gives a green security light backed by a check that doesn't rely on an AI's opinion (Goal 2). Decided from the goals + the standing requirement, not surfaced. Verified by watching the first CodeQL run; advanced setup is mutually exclusive with default setup (which was not-configured, so it runs clean). Closes #48.
- 2026-06-21 — [VISUAL GATE v1] Built the technical core of the visual-confirmation gate — the #1 goal gap (Trevor is the final eye on the visual result, but nothing let him SEE it). `tools/render_preview.py` (headless Blender, EEVEE) builds a representative example of an add-on's output (bevel: the rounded square as a solid tube; phyllotaxis: the 300-point sunflower spiral as instanced dots), auto-frames a top-down ortho camera over the geometry's bounding box, and renders an 800×800 PNG. A CI step renders both add-ons and uploads them as the `addon-previews` artifact (downloadable from any run) so Trevor can SEE what each add-on produces. `skills/visual-confirm.md` defines the gate: fires on a committed add-on change that moves a pixel; surfaces a plain-language "does this look right / is it what you meant? confirm / it's wrong" with the image — the HUMAN gate, distinct from the deterministic math gates, where "it's wrong" is a real defect even when the math passed. v1 = render + surface; pipeline auto-wiring deferred. RAG-corrected the 5.x engine id (`BLENDER_EEVEE`, not `EEVEE_NEXT`). Render step is `continue-on-error` (a preview, never a correctness gate). Independent Opus review PASS. CORRECTION: the first CI render (commit 9e028f1) FAILED — headless EEVEE aborted with "Couldn't open libEGL.so.1"; the runner lacked the EGL/GL stack and a display, so NO image was produced (the artifact was empty; `continue-on-error` masked it and I wrongly claimed I'd looked at the renders before checking). Fixed by adding `libegl1 libgles2 libgl1-mesa-dri xvfb` to the CI deps and wrapping the render in `xvfb-run` with `LIBGL_ALWAYS_SOFTWARE=1`. The render is only confirmed working once the orchestrator has actually downloaded the artifact and viewed the PNGs. CONFIRMED (commit 9bef768): the orchestrator downloaded the `addon-previews` artifact and LOOKED at both PNGs — bevel renders as a clean rounded-corner square, phyllotaxis as the golden-angle sunflower spiral of dots. Both read correctly; the visual gate v1 works end-to-end (render → CI artifact → human eye). The #1 goal gap (Trevor as the final eye on the visual result) now has a working mechanism.
- 2026-06-21 — [DOCS] Closed the onboarding gap (#43). README "Getting started" (run the checks locally — the full runner needs Blender, the three bpy-free pure gates run standalone with plain Python; what CI runs; `/build`; where add-ons live) plus a new plain-language `WHAT-IT-CHECKS.md` for the code-blind owner: what a green checkmark PROVES (geometry correct; the tool proved its own geometry tests catch broken work; secrets/deps/known-vuln scanned; ruff/bandit cleanliness) and what it does NOT — where Trevor is the final eye (does it LOOK right / is it what you meant → the visual-confirmation preview). Independent Opus review PASS (every command/path verified; the two trust-traps — implying the Blender gates are tamper-proven, and overstating dependency scanning — both avoided); applied its two honesty tweaks (the runner hard-fails without Blender rather than gracefully degrading; scoped the mutation claim to "geometry tests" to stay airtight re #50).
- 2026-06-21 — [GOVERNANCE] Codified the green-light guarantee + reconciled the pipeline docs to the real model (completes the 4th of Trevor's goal-answers: "believe the green light"). The system is direct-push — the orchestrator is the sole committer; no human PR merge, by design — so the green light is made trustworthy by the orchestrator itself: `orchestrator.md` now requires watching CI to green after every push and never knowingly leaving `main` red (fix-or-revert on red, fixed in-loop during an autonomous run — NOT a new run-stop; WRAP stays the only exit). Hard branch-protection required-checks would force a PR-merge flow that fights the autonomous loop — explicitly noted as not enabled, a deliberate model choice. While codifying, two reviewers caught that CLAUDE.md's Pipeline still described a PR-merge flow ("opens PR" / "PASS merges") that the orchestrator contradicts — a pre-existing doc bug; round 1 FAILed on it. Reconciled CLAUDE.md's Pipeline to the direct-push model (plus two stray "merge"→"commit to main" lines); round 2 BOTH PASS.
- 2026-06-23 — [FORCING] Commit gate (P1) — the first mechanical block on the 2026-06-22 forced-loop audit's worst gap: the review pipeline was markdown the model could silently skip (3 independent adversaries found ~86% of loop stages skippable; nothing made the model honor a reviewer's FAIL or stop an unreviewed commit). Built GIT-NATIVE (`.githooks/pre-commit`, not a Claude hook): it runs at true commit time and blocks unless `.review_state/verdict.json` records a PASS whose `tree_oid` equals `git write-tree` of the staged index — so it covers `git commit -a`/amend/pathspec/untracked and cannot be regex-bypassed or wrongly fire on a `grep "git commit"`. `tools/review_verdict.ps1` records the verdict from the reviewers' real result; `tools/check-gate.ps1` asserts the gate is active (wired into orchestrator step 6 — closes the silent-off trap); `tools/setup-hooks.ps1` activates it via `core.hooksPath`; `.gitattributes` forces LF so autocrlf can't corrupt the shebang; a CI integrity step reds `main` if the gate file is deleted/LF-corrupted; `DESIGN.md` records it as the live successor to the deleted Stop-hook with an HONEST enforcement boundary (does NOT stop `--no-verify`, a forged verdict, or an unconfigured clone — closing those is the program-driven loop P2, where the verdict comes from a reviewer agent call in control flow, not a hand). Dogfooded under the self-modification bar: round 1 BOTH reviewers FAIL (v1 was a Claude PreToolUse hook that string-matched `git commit` — missed `-a`/amend/untracked, could false-deny a grep) → redesigned git-native + `write-tree` key; round 2 BOTH FAIL (fail-OPEN when `git write-tree` failed and the verdict tree was empty they collided to `""=="" `; no activation check = the same silent-off trap that bit this repo) → added fail-closed `-z` guards + activation assertion + CI backstop + honest scoping + DESIGN entry; round 3 BOTH PASS. Proven end-to-end: a real `git commit` with no verdict aborted with HEAD unchanged; a corrupt verdict failed closed; staging a change after the verdict tripped the tree mismatch. The commit that introduces the gate self-certifies through it.
